//number =>  2 to power 53
// bigint
//string => "" or ''
// boolean => true/false
// null => standdalone value (object is data type)
// undefined =>  no values
// symbole => unique

let empName = "rahul" // this by default  string type
console.log(typeof(empName));
let rollNo = 1234; // this by default  number type
console.log(typeof(rollNo));
let empHight = 6.5; // this by default  number type
console.log(typeof(empHight));
let status = false; // this by default  boolean type
console.log(typeof(status)); // this is cut singh because of already defined in other js file
let gender; // undefined
console.log(typeof(gender));
let salary = null; //object
console.log(typeof(salary));
console.log(typeof salary); //<=== this types of decleration also work